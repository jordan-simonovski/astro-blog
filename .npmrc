# eslint-plugin-jsx-a11y@6.10.2 caps its eslint peer at v9, but this project runs
# eslint 10. The plugin's flat-config rules work fine on 10; this only relaxes the
# peer check so `npm install`/`npm ci` resolve. Remove once jsx-a11y supports v10.
legacy-peer-deps=true
